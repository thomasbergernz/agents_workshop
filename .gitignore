# The notebook writes its dataset here, relative to wherever it is run, and the
# whole point is that it is generated in about five seconds rather than cloned.
data/

.venv/
__pycache__/
.pytest_cache/
.ruff_cache/

# Part 13 writes a gateway configuration beside the notebook. It contains the
# literal ${OPENAI_API_KEY}, not a key, but there is no reason to commit it either.
aigw.yaml
